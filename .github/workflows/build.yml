name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'fix/[0-9]+.[0-9]+.x'
      - '[0-9]+.x'

jobs:
  build:
    uses: onecx/ci-java-lib/.github/workflows/cli-build.yml@v1
    secrets: inherit
